apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-cub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kargo-cub
  template:
    metadata:
      labels:
        app: kargo-cub
    spec:
      containers:
      - image: public.ecr.aws/nginx/nginx:1.27.4
        name: nginx
        volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: content
          readOnly: true
      volumes:
      - configMap:
          name: kargo-cub-content
        name: content
